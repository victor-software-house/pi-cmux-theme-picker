commit-msg:
  commands:
    commitlint:
      run: bunx --no -- commitlint --edit {1}

pre-push:
  commands:
    lockfile-sync:
      run: bun install --frozen-lockfile
    typecheck:
      run: bun run typecheck
