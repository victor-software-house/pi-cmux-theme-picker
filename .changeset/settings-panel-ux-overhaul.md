---
"pi-cmux-theme-picker": patch
---

Settings panel UX overhaul: color swatches for weight/tint/source settings, palette role mapping (remap which palette index feeds each semantic role), scoped per-theme settings with global vs per-theme toggle, override indicators, reset to defaults, and compact theme params in the status bar.
